
import * as Utils from '../utils/Utils';

describe('Utils suite test', () => {

    it('should test capitalize method', () => {
        expect(Utils.capitalize("capitalize")).toEqual("Capitalize")
    });
});
